class elementConfig extends canvasConfig{
    constructor(pos,length){
        super()
        this.pos = {
            X:pos.X,Y:pos.Y
        }
        this.length = {
            X:length.X,Y:length.Y
        }
        
    }

}